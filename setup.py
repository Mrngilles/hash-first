#!/usr/bin/env python

from setuptools import setup

setup(name='hash_first',
      version='0.1',
      description=(''),
      author='Marin Gilles',
      author_email='mrngilles@gmail.com',
      url='',
      packages=["hash_first"],
      setup_requires=["pytest-runner"],
      tests_require=["pytest"]
     )
